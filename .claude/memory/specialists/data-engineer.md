---
name: data-engineer-memory
description: "Specialist memory for data-engineer: schema decisions, migration patterns, indexing strategies, and data integrity lessons."
type: project
namespace: specialists/data-engineer
---

# Data Engineer — Specialist Memory

> Loaded ONLY when `@data-engineer` is active on the current task.
> Updated by `@technical-director` during consensus merges.

## Schema Decisions

_Fill during sessions — e.g. "Use ULID not UUID for ordered inserts"_

## Migration Patterns

_Fill during sessions — e.g. "Column removal: 3-phase (deprecate → backfill → drop)"_

## Indexing Strategy

_Fill during sessions — e.g. "Partial index on deleted_at IS NULL for soft-delete queries"_

## Recurring Mistakes to Avoid

_Fill during sessions — e.g. "Never run table-lock migrations during peak traffic hours"_
