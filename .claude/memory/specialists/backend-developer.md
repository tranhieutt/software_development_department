---
name: backend-developer-memory
description: "Specialist memory for backend-developer: API patterns, auth decisions, DB schema choices, service boundaries, and lessons learned from backend tasks."
type: project
namespace: specialists/backend-developer
---

# Backend Developer — Specialist Memory

> Loaded ONLY when `@backend-developer` is active on the current task.
> Updated by `@technical-director` during consensus merges.

## API Patterns Established

_Fill during sessions — e.g. "Use RS256 JWT, reason: multi-service support"_

## Architecture Decisions

_Fill during sessions — e.g. "Chose repository pattern over active record"_

## Known Gotchas

_Fill during sessions — e.g. "google-spreadsheet v5 breaks CJS, pin to v4"_

## Recurring Mistakes to Avoid

_Fill during sessions — e.g. "Always validate env vars at startup, not at call site"_
