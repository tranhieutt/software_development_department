---
name: fullstack-developer-memory
description: "Specialist memory for fullstack-developer: cross-layer integration patterns, vertical slice lessons, and handoff conventions between frontend and backend."
type: project
namespace: specialists/fullstack-developer
---

# Fullstack Developer — Specialist Memory

> Loaded ONLY when `@fullstack-developer` is active on the current task.
> Also loaded when acting as Circuit Breaker fallback for backend-developer or frontend-developer.
> Updated by `@technical-director` during consensus merges.

## Vertical Slice Patterns

_Fill during sessions — e.g. "Always lock API contract in ADR before writing UI code"_

## Integration Lessons

_Fill during sessions — e.g. "Use optimistic updates only when rollback is trivially reversible"_

## Handoff Conventions

_Fill during sessions — e.g. "Pass acceptance_criteria as array of strings in handoff contract"_

## Recurring Mistakes to Avoid

_Fill during sessions — e.g. "Never share DB connection pool between frontend SSR and API layer"_
