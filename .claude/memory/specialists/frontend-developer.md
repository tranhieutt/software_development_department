---
name: frontend-developer-memory
description: "Specialist memory for frontend-developer: component patterns, state management choices, accessibility decisions, and lessons learned from UI tasks."
type: project
namespace: specialists/frontend-developer
---

# Frontend Developer — Specialist Memory

> Loaded ONLY when `@frontend-developer` is active on the current task.
> Updated by `@technical-director` during consensus merges.

## Component Patterns Established

_Fill during sessions — e.g. "Use compound components for complex forms"_

## State Management Decisions

_Fill during sessions — e.g. "React Query for server state, Zustand for UI state"_

## Accessibility Baseline

_Fill during sessions — e.g. "All modals must trap focus, use FocusTrap lib"_

## Recurring Mistakes to Avoid

_Fill during sessions — e.g. "Always set explicit width/height on images to prevent CLS"_
