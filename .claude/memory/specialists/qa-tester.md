---
name: qa-tester-memory
description: "Specialist memory for qa-tester: test patterns, flaky test history, coverage gaps, edge cases found, and regression notes."
type: project
namespace: specialists/qa-tester
---

# QA Tester — Specialist Memory

> Loaded ONLY when `@qa-tester` is active on the current task.
> Updated by `@technical-director` during consensus merges.

## Test Patterns Established

_Fill during sessions — e.g. "Integration tests must hit real DB, no mocks"_

## Known Flaky Tests

_Fill during sessions — e.g. "auth_timeout_test: flaky on slow CI, add 500ms buffer"_

## Coverage Gaps

_Fill during sessions — e.g. "Payment error paths have 0% coverage as of 2026-04-16"_

## Edge Cases to Always Check

_Fill during sessions — e.g. "Empty pagination results, concurrent session tokens"_
